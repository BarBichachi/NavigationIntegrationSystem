<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="NavigationIntegrationSystem.UI.Views.Pages.IntegrationPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Integration" mc:Ignorable="d">

    <Grid Padding="16" RowDefinitions="Auto,*" RowSpacing="12">
        <!--  Header  -->
        <TextBlock FontSize="28" FontWeight="SemiBold" Text="Integration" />

        <!--  Work area  -->
        <Border Grid.Row="1" Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1" CornerRadius="12">

            <Grid RowDefinitions="Auto,*" RowSpacing="12">
                <Grid VerticalAlignment="Center" ColumnDefinitions="Auto,*" ColumnSpacing="12">
                    <TextBlock VerticalAlignment="Center" Opacity="0.7" Text="Sources:" />

                    <ItemsControl ItemsSource="{x:Bind ViewModel.ConnectedDevices, Mode=OneWay}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:IntegrationDeviceOptionViewModel">
                                <Border Margin="0,0,10,0" Padding="10,6"
                                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1" CornerRadius="10">
                                    <StackPanel Orientation="Horizontal" Spacing="10">
                                        <CheckBox Content="{x:Bind DisplayName}" IsChecked="{x:Bind IsVisible, Mode=TwoWay}" />
                                        <Button Command="{x:Bind ApplyToAllFieldsCommand}" Content="Apply to all" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>

                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Rows, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:IntegrationFieldRowViewModel">
                            <Border Margin="0,0,0,8" Padding="8"
                                    Background="{ThemeResource LayerFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="10">
                                <Grid MinHeight="48" ColumnDefinitions="80,60,200,*" ColumnSpacing="10">
                                    <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{x:Bind FieldName}" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Opacity="0.7" Text="{x:Bind Unit}" />
                                    <TextBlock Grid.Column="2" VerticalAlignment="Center" FontFamily="Consolas" FontWeight="SemiBold" Text="{x:Bind SelectedValueText}" />

                                    <ItemsControl Grid.Column="3" ItemsSource="{x:Bind VisibleSources, Mode=OneWay}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>

                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="vm:SourceCandidateViewModel">
                                                <Grid Width="200" Margin="0,0,12,0">
                                                    <RadioButton Content="{x:Bind DisplayText}" FontFamily="Consolas" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
