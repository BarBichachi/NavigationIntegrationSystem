<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="NavigationIntegrationSystem.UI.Views.Pages.LogsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="using:NavigationIntegrationSystem.UI.Views.Pages"
      xmlns:logging="using:NavigationIntegrationSystem.Infrastructure.Logging" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Grid Padding="16" RowDefinitions="Auto,*">
        <TextBlock FontSize="24" Text="Logs" />
        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Entries, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="logging:LogEntry">
                    <Grid ColumnDefinitions="150,60,*" ColumnSpacing="12">
                        <TextBlock Grid.Column="0" Text="{x:Bind Timestamp}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Level}" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Message}" TextWrapping="Wrap" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
