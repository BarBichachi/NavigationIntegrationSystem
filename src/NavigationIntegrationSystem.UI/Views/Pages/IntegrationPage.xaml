<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="NavigationIntegrationSystem.UI.Views.Pages.IntegrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Integration"
      mc:Ignorable="d">

    <Page.Resources>
        <!-- Numeric candidate: RadioButton + Text -->
        <DataTemplate x:Key="NumericCandidateTemplate" x:DataType="vm:NumericSourceCandidateViewModel">
            <Grid Width="150">
                <RadioButton VerticalAlignment="Center" IsChecked="{x:Bind IsSelected, Mode=OneWay}" Click="OnSourceRadioButtonClicked">
                    <RadioButton.Content>
                        <TextBlock VerticalAlignment="Center" FontFamily="Consolas" FontSize="14" Text="{x:Bind DisplayText, Mode=OneWay}" />
                    </RadioButton.Content>
                </RadioButton>
            </Grid>
        </DataTemplate>

        <!-- Manual candidate: RadioButton + TextBox (enabled only when selected) -->
        <DataTemplate x:Key="ManualCandidateTemplate" x:DataType="vm:ManualSourceCandidateViewModel">
            <Grid Width="150">
                <Grid VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="100" />
                    </Grid.ColumnDefinitions>

                    <RadioButton Grid.Column="0" Width="32" Padding="0" HorizontalAlignment="Left" VerticalAlignment="Center"
                 IsChecked="{x:Bind IsSelected, Mode=OneWay}" Click="OnSourceRadioButtonClicked" />

                    <TextBox Grid.Column="1" Margin="4,0,0,0" FontFamily="Consolas" FontSize="14"
             Text="{x:Bind Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             IsEnabled="{x:Bind IsSelected, Mode=OneWay}"
             PlaceholderText="Value" />
                </Grid>
            </Grid>
        </DataTemplate>

        <!-- Chooses template by candidate type -->
        <vm:IntegrationCandidateTemplateSelector x:Key="IntegrationCandidateTemplateSelector"
                                                NumericTemplate="{StaticResource NumericCandidateTemplate}"
                                                ManualTemplate="{StaticResource ManualCandidateTemplate}" />
    </Page.Resources>

    <!-- Page layout: Title + Integration surface -->
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" FontSize="28" FontWeight="SemiBold" Text="Integration" />

        <!-- Main integration surface -->
        <Border Grid.Row="1" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12">
            <Grid RowSpacing="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Active sources selector (visibility + apply-to-all) -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Active Sources (Max 3):" />

                    <!-- Scrollable chips: connected sources only -->
                    <ScrollViewer Grid.Column="1" HorizontalAlignment="Right" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{x:Bind ViewModel.ConnectedDevices, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="12" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="vm:IntegrationDeviceOptionViewModel">
                                    <Border Padding="8,4" Background="{ThemeResource ControlFillColorDefaultBrush}" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <CheckBox VerticalAlignment="Center" FontWeight="Bold" Content="{x:Bind DisplayName}" IsChecked="{x:Bind IsVisible, Mode=TwoWay}" />
                                            <Rectangle Width="1" Height="24" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />
                                            <Button Padding="4,4" FontSize="12" Foreground="White" Content="Apply to All" Background="{ThemeResource SystemAccentColor}" Style="{StaticResource AccentButtonStyle}"
                                                    Command="{x:Bind ApplyToAllFieldsCommand}" IsEnabled="{x:Bind IsVisible, Mode=OneWay}" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>

                <!-- Divider -->
                <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

                <!-- Parameters table -->
                <ListView Grid.Row="2" ItemsSource="{x:Bind ViewModel.Rows, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <!-- Header row -->
                    <ListView.Header>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Margin="16,0" FontWeight="SemiBold" Opacity="0.6" Text="Parameter" />
                            <TextBlock Grid.Column="1" HorizontalAlignment="Center" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColorLight2}" Text="Integrated Output" />

                            <!-- Spacer -->
                            <Grid Grid.Column="2"/>

                            <!-- Source columns (visible-only) -->
                            <ItemsControl Grid.Column="3" ItemsSource="{x:Bind ViewModel.ConnectedDevices, Mode=OneWay}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="vm:IntegrationDeviceOptionViewModel">
                                        <Grid Width="150" Visibility="{x:Bind IsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock FontWeight="SemiBold" Opacity="0.6" Text="{x:Bind DisplayName}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </ListView.Header>

                    <!-- Make each row stretch full width -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>

                    <!-- One row per integration field -->
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:IntegrationFieldRowViewModel">
                            <Border Margin="0,0,0,10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8">
                                <Grid MinHeight="50">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="70" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Field name + unit -->
                                    <StackPanel Grid.Column="0" Margin="16,0" VerticalAlignment="Center">
                                        <TextBlock FontSize="15" FontWeight="Bold" Text="{x:Bind FieldName}" />
                                        <TextBlock FontSize="11" Opacity="0.5" Text="{x:Bind Unit}" />
                                    </StackPanel>

                                    <!-- Integrated output -->
                                    <Grid Grid.Column="1">
                                        <Border Background="{ThemeResource SystemAccentColor}" CornerRadius="6" Opacity="0.1" />
                                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="Consolas" FontSize="20" FontWeight="Bold" Foreground="{ThemeResource SystemAccentColorLight3}"
                                                   Text="{x:Bind SelectedValueText, Mode=OneWay}" />
                                    </Grid>

                                    <!-- Spacer -->
                                    <Grid Grid.Column="2"/>

                                    <!-- Per-source candidates -->
                                    <ItemsControl Grid.Column="3" ItemsSource="{x:Bind VisibleSources, Mode=OneWay}" ItemTemplateSelector="{StaticResource IntegrationCandidateTemplateSelector}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
