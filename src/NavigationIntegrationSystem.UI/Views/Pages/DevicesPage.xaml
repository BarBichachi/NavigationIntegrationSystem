<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="NavigationIntegrationSystem.UI.Views.Pages.DevicesPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:panes="using:NavigationIntegrationSystem.UI.Views.Panes" xmlns:controls="using:NavigationIntegrationSystem.UI.Views.Controls" xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Devices.Cards" x:Name="RootPage" mc:Ignorable="d">

    <!--  Outer SplitView fills the page edge-to-edge. Page header (title + Import/Export) and footer (config path + Open folder) used to be sibling Grid rows of the SplitView, which meant the LightDismissOverlay only covered the cards row when the pane opened (visible as a "framed" dim with bright header/footer/padding around it). They're now INSIDE SplitView.Content so the dim covers the whole page area  -->
    <SplitView DisplayMode="Overlay"
               IsPaneOpen="{x:Bind ViewModel.IsPaneOpen, Mode=TwoWay}"
               LightDismissOverlayMode="On" OpenPaneLength="520" PaneClosing="OnPaneClosing" PanePlacement="Right">

        <SplitView.Content>
            <Grid Padding="16" RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Page header: title + Import/Export  -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock FontSize="28" FontWeight="SemiBold" Text="Devices" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                        <Button Command="{x:Bind ViewModel.ImportDevicesConfigCommand}" Content="Import Settings" />
                        <Button Command="{x:Bind ViewModel.ExportDevicesConfigCommand}" Content="Export Settings" />
                    </StackPanel>
                </Grid>

                <!--  Devices list: each item is a card  -->
                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Devices, Mode=OneWay}" SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:DeviceCardViewModel">
                            <Border Margin="0,0,0,10" Padding="14"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{x:Bind Status, Mode=OneWay, Converter={StaticResource DeviceStatusToBrushConverter}}"
                                    BorderThickness="1" CornerRadius="12">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock FontSize="18" Text="{x:Bind DisplayName}" />
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8" VerticalAlignment="Center">
                                        <controls:DeviceModeChip Label="{x:Bind ModeLabel, Mode=OneWay}" Severity="{x:Bind ModeSeverity, Mode=OneWay}" />
                                        <TextBlock FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"
                                                   Foreground="{x:Bind Status, Mode=OneWay, Converter={StaticResource DeviceStatusToBrushConverter}}"
                                                   Text="{x:Bind Status, Mode=OneWay}" />
                                    </StackPanel>
                                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Margin="0,12,0,0" Orientation="Horizontal" Spacing="10">
                                        <Button Command="{x:Bind ToggleConnectCommand}" IsEnabled="{x:Bind CanToggleConnect, Mode=OneWay}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <SymbolIcon Symbol="Link" />
                                                <TextBlock Text="{x:Bind ConnectButtonText, Mode=OneWay}" />
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind OpenSettingsCommand}" Visibility="{x:Bind IsSettingsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <SymbolIcon Symbol="Setting" />
                                                <TextBlock Text="Settings" />
                                            </StackPanel>
                                        </Button>
                                        <Button Command="{x:Bind OpenInspectCommand}" Visibility="{x:Bind IsInspectVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <SymbolIcon Symbol="Preview" />
                                                <TextBlock Text="Inspect" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                    <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,8,0,0" Orientation="Horizontal" Spacing="6"
                                                Visibility="{x:Bind LastError, Mode=OneWay, Converter={StaticResource StringNullOrEmptyToVisibilityConverter}}">
                                        <FontIcon Glyph="&#xE7BA;" FontSize="14" VerticalAlignment="Center"
                                                  Foreground="{x:Bind Status, Mode=OneWay, Converter={StaticResource DeviceStatusToBrushConverter}}" />
                                        <TextBlock Text="{x:Bind LastError, Mode=OneWay}" Opacity="0.85" VerticalAlignment="Center"
                                                   Foreground="{x:Bind Status, Mode=OneWay, Converter={StaticResource DeviceStatusToBrushConverter}}" />
                                    </StackPanel>

                                    <!--  Auto-reconnect countdown ("Reconnecting in 5s..."). Visible only while a backoff loop is running; ticks at 1Hz so the user sees a live countdown and has time to click "Cancel reconnect" before the next attempt fires  -->
                                    <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,4,0,0" Orientation="Horizontal" Spacing="6"
                                                Visibility="{x:Bind IsAutoReconnecting, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <FontIcon Glyph="&#xE895;" FontSize="14" VerticalAlignment="Center" Opacity="0.7" />
                                        <TextBlock Text="{x:Bind ReconnectStatusText, Mode=OneWay}" Opacity="0.85" VerticalAlignment="Center" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!--  Page footer  -->
                <Grid Grid.Row="2" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" Opacity="0.7" Text="{x:Bind ViewModel.DevicesConfigPath, Mode=OneWay}" />
                    <HyperlinkButton Grid.Column="1" Command="{x:Bind ViewModel.OpenDevicesConfigFolderCommand}" Content="Open config folder" />
                </Grid>
            </Grid>
        </SplitView.Content>

        <SplitView.Pane>
            <Grid Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Pane header: selected device name + mode chip. Uses regular {Binding} (not x:Bind) for the chip props because x:Bind on a chained nullable path (ViewModel.SelectedDevice.ModeSeverity) tripped a hard XamlCompiler failure at the root-page level; runtime {Binding} resolves it cleanly  -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                        <TextBlock FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" Text="{x:Bind ViewModel.SelectedDevice.DisplayName, Mode=OneWay}" />
                        <controls:DeviceModeChip Label="{Binding ViewModel.SelectedDevice.ModeLabel, ElementName=RootPage}" Severity="{Binding ViewModel.SelectedDevice.ModeSeverity, ElementName=RootPage}" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Height="1" Margin="0,12,0,12" Background="{ThemeResource CardStrokeColorDefaultBrush}" />

                <Grid Grid.Row="2" Margin="0,0,0,12">
                    <panes:DeviceSettingsPaneView x:Name="DeviceSettingsPaneView" DataContext="{x:Bind ViewModel.CurrentSettingsPane, Mode=OneWay}" Visibility="{x:Bind ViewModel.PaneMode, Mode=OneWay, Converter={StaticResource PaneModeToSettingsVisibilityConverter}}" />
                    <panes:DeviceInspectPaneView DataContext="{x:Bind ViewModel.CurrentInspectPane, Mode=OneWay}" Visibility="{x:Bind ViewModel.PaneMode, Mode=OneWay, Converter={StaticResource PaneModeToInspectVisibilityConverter}}" />
                </Grid>
            </Grid>
        </SplitView.Pane>
    </SplitView>
</Page>
