<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Panes.DeviceInspectPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="using:NavigationIntegrationSystem.UI.Views.Panes"
             xmlns:sub="using:NavigationIntegrationSystem.UI.Views.Panes.SubViews"
             xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Devices.Panes"
             mc:Ignorable="d">

    <UserControl.Resources>
        <!-- One DataTemplate per concrete inspect pane VM subclass. The ContentControl + InspectPaneTemplateSelector pair instantiates exactly the right sub-view for the active device; only one is ever in the visual tree -->
        <DataTemplate x:Key="GenericTemplate" x:DataType="vm:GenericInspectPaneViewModel">
            <sub:GenericInspectView />
        </DataTemplate>

        <DataTemplate x:Key="Vn310Template" x:DataType="vm:Vn310InspectPaneViewModel">
            <sub:Vn310InspectView />
        </DataTemplate>

        <local:InspectPaneTemplateSelector x:Key="InspectPaneSelector"
                                           GenericTemplate="{StaticResource GenericTemplate}"
                                           Vn310Template="{StaticResource Vn310Template}" />
    </UserControl.Resources>

    <ContentControl Content="{x:Bind ViewModel, Mode=OneWay}"
                    ContentTemplateSelector="{StaticResource InspectPaneSelector}"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />
</UserControl>
