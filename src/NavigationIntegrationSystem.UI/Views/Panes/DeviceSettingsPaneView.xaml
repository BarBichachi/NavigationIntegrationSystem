<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Panes.DeviceSettingsPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sub="using:NavigationIntegrationSystem.UI.Views.Panes.SubViews"
             mc:Ignorable="d">

    <ScrollViewer HorizontalContentAlignment="Stretch" HorizontalScrollBarVisibility="Disabled">
        <StackPanel x:Name="RootPanel" HorizontalAlignment="Stretch" Spacing="14">

            <TextBlock FontSize="18" Text="Device Settings" />

            <!-- Real Device Settings -->
            <sub:RealDeviceSettingsView Visibility="{x:Bind ViewModel.IsPlaybackDevice, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}" />

            <!-- Playback Settings -->
            <sub:PlaybackSettingsView Visibility="{x:Bind ViewModel.IsPlaybackDevice, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

        </StackPanel>
    </ScrollViewer>
</UserControl>