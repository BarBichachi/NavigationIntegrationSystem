<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Panes.DeviceSettingsPaneView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <ScrollViewer HorizontalContentAlignment="Stretch" HorizontalScrollBarVisibility="Disabled">
        <StackPanel HorizontalAlignment="Stretch" Spacing="14">

            <TextBlock FontSize="18" Text="Device Settings" />

            <!--  General  -->
            <Border Padding="12" BorderThickness="1" CornerRadius="10">
                <StackPanel HorizontalAlignment="Stretch" Spacing="10">

                    <TextBlock FontSize="16" Opacity="0.8" Text="General" />

                    <Grid HorizontalAlignment="Stretch" ColumnSpacing="12" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Connection kind" />
                        <ComboBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" ItemsSource="{Binding ConnectionKinds}" SelectedItem="{Binding DraftConfig.Connection.Kind, Mode=TwoWay}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Auto reconnect" />
                        <CheckBox Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding DraftConfig.AutoReconnect, Mode=TwoWay}" />
                    </Grid>

                </StackPanel>
            </Border>

            <!--  UDP  -->
            <Border Padding="12" BorderThickness="1" CornerRadius="10" Visibility="{Binding DraftConfig.Connection.Kind, Converter={StaticResource ConnectionKindToVisibilityConverter}, ConverterParameter=Udp}">
                <StackPanel HorizontalAlignment="Stretch" Spacing="10">
                    <TextBlock FontSize="16" Opacity="0.8" Text="UDP" />

                    <Grid HorizontalAlignment="Stretch" ColumnSpacing="12" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Remote IP" />
                        <TextBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" Text="{Binding DraftConfig.Connection.Udp.RemoteIp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Remote port" />
                        <NumberBox Grid.Row="1" Grid.Column="1" MinWidth="220" HorizontalAlignment="Stretch" SpinButtonPlacementMode="Inline"
                                   Value="{Binding DraftConfig.Connection.Udp.RemotePort, Mode=TwoWay}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Local IP" />
                        <TextBox Grid.Row="2" Grid.Column="1" HorizontalAlignment="Stretch" Text="{Binding DraftConfig.Connection.Udp.LocalIp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Local port" />
                        <NumberBox Grid.Row="3" Grid.Column="1" MinWidth="220" HorizontalAlignment="Stretch" SpinButtonPlacementMode="Inline"
                                   Value="{Binding DraftConfig.Connection.Udp.LocalPort, Mode=TwoWay}" />
                    </Grid>
                </StackPanel>
            </Border>

            <!--  TCP  -->
            <Border Padding="12" BorderThickness="1" CornerRadius="10" Visibility="{Binding DraftConfig.Connection.Kind, Converter={StaticResource ConnectionKindToVisibilityConverter}, ConverterParameter=Tcp}">
                <StackPanel HorizontalAlignment="Stretch" Spacing="10">
                    <TextBlock FontSize="16" Opacity="0.8" Text="TCP" />

                    <Grid HorizontalAlignment="Stretch" ColumnSpacing="12" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Host" />
                        <TextBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" Text="{Binding DraftConfig.Connection.Tcp.Host, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Port" />
                        <NumberBox Grid.Row="1" Grid.Column="1" MinWidth="220" HorizontalAlignment="Stretch" SpinButtonPlacementMode="Inline"
                                   Value="{Binding DraftConfig.Connection.Tcp.Port, Mode=TwoWay}" />
                    </Grid>
                </StackPanel>
            </Border>

            <!--  Serial  -->
            <Border Padding="12" BorderThickness="1" CornerRadius="10" Visibility="{Binding DraftConfig.Connection.Kind, Converter={StaticResource ConnectionKindToVisibilityConverter}, ConverterParameter=Serial}">
                <StackPanel HorizontalAlignment="Stretch" Spacing="10">
                    <TextBlock FontSize="16" Opacity="0.8" Text="Serial" />

                    <Grid HorizontalAlignment="Stretch" ColumnSpacing="12" RowSpacing="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Line kind" />
                        <ComboBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch" ItemsSource="{Binding SerialLineKinds}" SelectedItem="{Binding DraftConfig.Connection.Serial.SerialLineKind, Mode=TwoWay}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="COM port" />
                        <TextBox Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch" Text="{Binding DraftConfig.Connection.Serial.ComPort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Opacity="0.7" Text="Baud rate" />
                        <NumberBox Grid.Row="2" Grid.Column="1" MinWidth="220" HorizontalAlignment="Stretch" SpinButtonPlacementMode="Inline"
                                   Value="{Binding DraftConfig.Connection.Serial.BaudRate, Mode=TwoWay}" />
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>