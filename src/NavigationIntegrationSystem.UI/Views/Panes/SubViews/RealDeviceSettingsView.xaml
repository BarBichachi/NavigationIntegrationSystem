<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Panes.SubViews.RealDeviceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Devices.Panes"
             mc:Ignorable="d">

    <StackPanel Spacing="14">
        <!-- General Settings -->
        <Border Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
            <StackPanel Spacing="10">
                <TextBlock FontSize="16" Opacity="0.8" Text="General" />
                <Grid ColumnSpacing="12" RowSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Connection kind" VerticalAlignment="Center" Opacity="0.7"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch"
                              ItemsSource="{Binding ConnectionKinds}"
                              SelectedItem="{Binding Draft.ConnectionKind, Mode=TwoWay}" />

                    <TextBlock Grid.Row="1" Text="Auto reconnect" VerticalAlignment="Center" Opacity="0.7"/>
                    <CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding Draft.AutoReconnect, Mode=TwoWay}" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- UDP Settings -->
        <Border Padding="12" BorderThickness="1" CornerRadius="10" Visibility="{Binding Draft.IsUdpSelected, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Spacing="10">
                <TextBlock FontSize="16" Opacity="0.8" Text="UDP" />
                <Grid ColumnSpacing="12" RowSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Remote IP" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Draft.UdpRemoteIp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="1" Text="Remote Port" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Draft.UdpRemotePortText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="2" Text="Local IP" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Draft.UdpLocalIp, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="3" Text="Local Port" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Draft.UdpLocalPortText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- TCP Settings -->
        <Border Padding="12" BorderThickness="1" CornerRadius="10" Visibility="{Binding Draft.IsTcpSelected, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Spacing="10">
                <TextBlock FontSize="16" Opacity="0.8" Text="TCP" />
                <Grid ColumnSpacing="12" RowSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Host" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Draft.TcpHost, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="1" Text="Port" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Draft.TcpPortText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Serial Settings -->
        <Border Padding="12" BorderThickness="1" CornerRadius="10" Visibility="{Binding Draft.IsSerialSelected, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel Spacing="10">
                <TextBlock FontSize="16" Opacity="0.8" Text="Serial" />
                <Grid ColumnSpacing="12" RowSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Line Kind" VerticalAlignment="Center" Opacity="0.7"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" HorizontalAlignment="Stretch"
                              ItemsSource="{Binding SerialLineKinds}"
                              SelectedItem="{Binding Draft.SerialLineKind, Mode=TwoWay}" />

                    <TextBlock Grid.Row="1" Text="COM Port" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Draft.SerialComPort, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Grid.Row="2" Text="Baud Rate" VerticalAlignment="Center" Opacity="0.7"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Draft.SerialBaudRateText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>