<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Panes.SubViews.GenericInspectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Devices.Cards"
             mc:Ignorable="d">

    <UserControl.Resources>
        <DataTemplate x:Key="InspectFieldRowTemplate" x:DataType="vm:InspectFieldViewModel">
            <Grid Padding="0,4" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="70" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Bind Name}" />
                <TextBlock Grid.Column="1" Text="{x:Bind ValueText, Mode=OneWay}" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="2" HorizontalAlignment="Right" Opacity="0.7" Text="{x:Bind Unit}" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Margin="0,0,0,8" Opacity="0.7" Text="Inspect (all fields)" />
        <ListView Grid.Row="1" ItemsSource="{Binding Fields}" ItemTemplate="{StaticResource InspectFieldRowTemplate}" />
    </Grid>
</UserControl>
