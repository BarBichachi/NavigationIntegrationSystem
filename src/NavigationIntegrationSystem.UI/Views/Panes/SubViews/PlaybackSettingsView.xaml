<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Panes.SubViews.PlaybackSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Devices.Panes"
             mc:Ignorable="d">

    <StackPanel Spacing="14">
        <!-- Playback Settings -->
        <Border Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
            <StackPanel Spacing="10">
                <TextBlock FontSize="16" Opacity="0.8" Text="Playback Source" />

                <Grid ColumnSpacing="12" RowSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="160" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="File Path" VerticalAlignment="Center" Opacity="0.7"/>
                    <Grid Grid.Row="0" Grid.Column="1" ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" IsReadOnly="True" PlaceholderText="Select .csv file"
                                 Text="{Binding Draft.PlaybackFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Button Grid.Column="1" Command="{Binding BrowseFileCommand}">
                            <SymbolIcon Symbol="BrowsePhotos" />
                        </Button>
                    </Grid>

                    <TextBlock Grid.Row="1" Text="Frequency (Hz)" VerticalAlignment="Center" Opacity="0.7"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch"
                              ItemsSource="{Binding Frequencies}"
                              SelectedItem="{Binding Draft.PlaybackFrequency, Mode=TwoWay}" />

                    <TextBlock Grid.Row="2" Text="Loop" VerticalAlignment="Center" Opacity="0.7"/>
                    <CheckBox Grid.Row="2" Grid.Column="1" IsChecked="{Binding Draft.PlaybackLoop, Mode=TwoWay}" />
                </Grid>
            </StackPanel>
        </Border>

        <!-- Tools -->
        <Border Padding="12" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="10">
            <StackPanel Spacing="10">
                <TextBlock FontSize="16" Opacity="0.8" Text="Tools" />
                <Button HorizontalAlignment="Left" Command="{Binding ExportTemplateCommand}" Content="Export CSV Template" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>