<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="NavigationIntegrationSystem.UI.Views.Controls.PlaybackTrayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:NavigationIntegrationSystem.UI.ViewModels.Playback"
             mc:Ignorable="d">

    <Grid Visibility="{x:Bind ViewModel.IsVisible, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
          Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="16,8">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
            <Button Command="{x:Bind ViewModel.TogglePlayCommand}" ToolTipService.ToolTip="{x:Bind ViewModel.PlayPauseTooltip, Mode=OneWay}">
                <SymbolIcon Symbol="{x:Bind ViewModel.PlayPauseIcon, Mode=OneWay}" />
            </Button>
            <Button Command="{x:Bind ViewModel.StopCommand}" ToolTipService.ToolTip="Stop">
                <SymbolIcon Symbol="Stop" />
            </Button>
        </StackPanel>

        <Slider Grid.Column="1" Margin="16,0" VerticalAlignment="Center" 
                Minimum="0" Maximum="{x:Bind ViewModel.TotalLineCount, Mode=OneWay}" 
                Value="{x:Bind ViewModel.SeekValue, Mode=TwoWay}" />

        <TextBlock Grid.Column="2" VerticalAlignment="Center" FontFamily="Consolas" Opacity="0.8" 
                   Text="{x:Bind ViewModel.ProgressText, Mode=OneWay}" />
    </Grid>
</UserControl>